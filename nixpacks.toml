providers = ["node", "python"]

[phases.setup]
nixPkgs = ["nodejs", "npm", "python311", "postgresql"]

[phases.install]
cmds = [
    "npm install",
    "pip install -r requirements.txt"
]

[start]
cmd = "python app.py"
