Flask==2.3.2
psycopg2-binary==2.9.6
python-dotenv==1.0.0
Werkzeug==2.3.6
gunicorn==21.2.0
```

## Alternative: Use a Procfile

If you don't want to use gunicorn, create a file named `Procfile` (no extension) in your repository root:
```
web: python app.py
